# Copy to server/.env on your prod box and fill in real values.
# Loaded by `node --env-file=server/.env server/dist/index.js`
# or by the systemd unit via EnvironmentFile=.

DB_PATH=/data/danger-close.db
PORT=8080
SESSION_SECRET=replace-with-a-long-random-hex-string
NODE_ENV=production
